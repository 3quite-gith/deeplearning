假设你有一个神经网络：
输入层有 n 个特征
第 1 个隐藏层有 2 个神经元（为了简化），使用 sigmoid 或 tanh 激活
输出层做分类

如果你把所有权重初始化为 0，并且所有偏置初始化为 0，会发生什么？
2. 前向传播时
设输入样本为 x
第一隐藏层：
神经元 1：
z1=w1⋅x+b1​，其中 w1=[0,0,…,0]，b1=0，所以 z1=0
神经元 2：
z2=w2⋅x+b2，其中 w2=[0,0,…,0]，b1=0，所以 z2=0
激活函数输出：a1=σ(z1)，a2=σ(z2)
因为 z1=z2，所以a1=a2。也就是说，同一层的所有神经元在前向传播中输出完全相同。
